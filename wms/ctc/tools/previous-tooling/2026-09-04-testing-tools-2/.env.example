# Copy to `.env` beside this file and fill in. Auto-loaded, never printed.
# Needed only by the three scripts that talk to Cin7 directly
# (find-cin7-sales-order.sh, survey-cin7-orders.sh, survey-contact-groups.sh).
#
# Cin7 is CTC's PRODUCTION system. Every script here is GET-only against it.
# Never commit or share a filled-in `.env`.
CIN7_USERNAME=
CIN7_API_KEY=
