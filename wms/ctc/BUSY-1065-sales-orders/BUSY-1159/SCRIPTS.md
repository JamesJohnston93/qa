# BUSY-1159 scripts

Scripts written during this ticket's testing. Ticket specific ones live in `scripts/` beside this file. Anything that turns out to be useful to another ticket gets promoted to `../../tools/` and moved to `../../tools/SCRIPTS-INDEX.md`.

**Check this file and the root index before writing anything new.**

| Script | TC | Purpose | What it does NOT check | Read only | Reviewed by |
|---|---|---|---|---|---|
| `scripts/wait-for-so-cycle.sh` | TC1, TC3, TC4 | Polls the SO poller log group until a cycle-complete event appears after a given time, so a case can wait for the next scheduled cycle without a manual invoke | What the cycle did, only that one happened | yes | not yet |
| `scripts/capture-tc1-evidence.sh` | TC1, TC1b, TC3, TC4 | Captures poller cycle-complete events and one reference's Manhattan send outcome since a given time, printing structured fields only, never raw XML | Whether the order is correct end to end; pair with `inspect-ctc-order.sh` and `check-latency.sh` | yes | not yet |
| `scripts/check-latency.sh` | TC1b | Computes the gap between a Cin7 `modifiedDate` and a shipment's `wmsSentAt`, against the 5 minute ceiling | Which log group the delay sits in if it fails; that needs a manual read | yes, no AWS calls at all | not yet |
| `scripts/address-field-lengths.sh` | TC18 | Prints the ADDRESS row's field lengths for one order, never the values, so a truncation or shape case can be checked without a customer name, email or address ever leaving the script | Whether the content is correct, only its length | yes | not yet |
| `scripts/list-transaction-rows.sh` | TC21b | Lists TRANSACTION row SKs and idempotencyId for one order's origin, redaction-safe (never the customerEmail/addressChanges/orderInfo also stored on that row) | Whether a dedupe actually happened, only what is stored; pair two runs before/after an invoke | yes | not yet |
| `scripts/find-picked-stage-orders.sh` | TC14 | One Cin7 GET for orders currently at Fully Picked/Partially Picked, printing company name per order so a wholesale account can be told apart from an ECOM one at a glance | Contact group; company name is a fast proxy, a firm call still needs find-cin7-sales-order.sh --with-contact | yes | not yet |
| `scripts/putevents-vs-populator.sh` | TC22 (Gate B) | Compares the poller's `Pushed` PutEvents lines against the eda-queue-populator's `Received event` lines for the same window, matched by idempotencyId, so a shortfall shows up directly instead of from counts alone | Whether the code checks the PutEvents response; only shows whether any record has actually gone missing so far | yes | not yet |
| `scripts/hash-segment-check.sh` | TC13b | Harvests idempotencyId's trailing segment across a set of references (via `list-transaction-rows.sh`) and reports width, character set, presence rate, and whether it varies by order; compares same-reference-same-modifiedDate pairs if any exist | Stability against identical input when no such pair exists, only the weaker cross-order variance claim | yes | not yet |
| `scripts/survey-multisize-styles.sh` | TC6b, TC6c | Scans both `staging-orders-v2` (all ITEM rows for CTC-origin orders, pre-shipping-filter) and `staging-shipments` (company=CTC ITEM rows, post DIGITAL/INSTORE filter), groups each by (order PK, lineItemId), reports groups with 2+ distinct sku (multi-size style candidates) on each table, population counts, largest distinct-sku count, and a per-order comparison of orders-v2 vs shipments item counts with deliveryMethod breakdown, flagging any difference not explained by the known DIGITAL/INSTORE exclusion | Cin7 calls, so cannot see a zero-quantity size that produced no stored row at all in either table, and does not itself confirm a reconciliation against the Cin7 line total | yes | not yet |
| `scripts/check-manhattan-populator-message-group.sh` | TC16b | Reads `staging-shipping-manhattan-manhattan-eda-queue-populator`'s log group over a window and extracts only detail-type, origin, PK and message_group_id per event, never the full payload (that log group also carries shippingAddress email/name/street fields) | Does not touch the Manhattan sender queue itself, and does not show the SQS message's actual assigned group after the populator's own fallback logic runs, only what was present on the incoming event | yes | not yet |
| `scripts/check-skipped-no-sizes.sh` | TC6d | Sweeps every `Cin7SOPollerCycleComplete` line in the poller's full retained CloudWatch history (defaults to the log group's earliest retained event) and reports the `skippedNoSizes` counter's distribution: cycle count, non-zero count, largest value, and the non-zero cycles' timestamp/ordersFetched/created | Does not identify which order within a non-zero cycle triggered the counter (needs a separate per-order read). Does not distinguish a genuinely new counter from a typo. Retention is confirmed unlimited on this log group but that is not the same as "since the CTC build began" | yes | not yet |

## Notes

A row here is not a claim that the script is correct. The Reviewed column is the only thing that says a human has read it. Until that column is filled, a passing run proves the script ran, not that the system behaved.
